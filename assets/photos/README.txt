Player photos (optional)
========================
Drop local player images here named by jersey number or player_id:
  assets/photos/<player_id>.png   (preferred)
  assets/photos/<jersey>.png      (fallback)

The formation pitch view will use them automatically. If a photo is
missing, a clean numbered token is drawn instead — the layout never
breaks and no external URLs are used.
